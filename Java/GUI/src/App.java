
public class App {
    public static void main(String[] args) throws Exception {
        // JFrame = a GUI window to add components to

        /*
        JFrame  frame = new JFrame(); //creates a frame
        frame.setTitle("JFrame Haha");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);//exit out of app
        frame.setResizable(false);//prevents from resizing
        frame.setSize(400,400);//sets height and width of the frame
        frame.setVisible(true); //makes frame visible
        ImageIcon image = new ImageIcon("GUI/logo.png");
        frame.setIconImage(image.getImage()); //change icon of frame
        frame.getContentPane().setBackground(new Color(38,50,87));
        */

        MyFrame myFrame = new MyFrame();
    }
}
